# Независимая QA: соединения, Steam и отказы сохранения

Дата: 22 сентября 2026. Проверенная родительская версия: `8586d82d9596e351a01488cac11c797edd9e650b`, `C:/Normal/Programs/Game`. Проверка проведена по просьбе интегратора в рамках продолжения работы. Навык: code-review.

**Вердикт: REQUEST CHANGES. Найдено 1 P1 и 3 P2; P0 не найдено.** Высокая уверенность в описанных ветках кода; сетевые и файловые сценарии ниже не исполнялись этим ревьюером.

Прочитаны целиком `Assets/Scripts/HotelSession.cs`, `HotelSaveStore.cs`, `HotelSteam.cs`, `Assets/Steam/Runtime/HotelSteamTransport.cs` и `SteamPacket.cs`; проверены связанные вызовы в HotelUI/HotelGame/HotelSimulation, сохранения в Editor/HotelSimulationTests, документация и offline-проверки Steam. Порядок событий проверен по установленным исходникам NGO **2.7.0**, а не предположен по названию callback.

Весь исходный код читался из родительского проекта. Runtime, родительский worktree, Unity и игровые процессы не изменялись/не запускались; push не выполнялся. Единственный результат в собственной рабочей копии — этот отчёт. Шесть закрытых замечаний из `qa-independent.md` не переоткрываются: ниже другие условия отказа. Начатые параллельно презентационные изменения родителя не входят в ревью; ссылки на потребителей относятся к `8586d82`.

## F01 — P1: после отказа транспорта актуальный мир хоста больше нельзя сохранить

**Основное место:** `Worst Hotel Ever/Assets/Scripts/HotelSession.cs:62`.

Связанные места: `HotelSession.cs:19–20,182–188,239–254`; `HotelGame.cs:224`; `HotelSteamTransport.cs:105–108,174–179`.

Обработчик `OnTransportFailure` меняет только текст статуса и Connecting. В NGO `NetworkConnectionManager.TransportFailureEventHandler` сначала вызывает этот callback, затем сам выполняет `NetworkManager.Shutdown(true)`. При завершении shutdown NGO сбрасывает роль локального клиента. После этого `Session.IsHost` становится false, поэтому `Save()` немедленно возвращает false, хотя `Simulation` и актуальный `State` ещё могут находиться в памяти. Обработчик отключения локального хоста не сохраняет мир. `Game.CanQuit` также не пытается сохранить бывшего хоста, а следующая попытка входа через меню вызывает `Disconnect(false)` и окончательно очищает состояние.

Это не прежняя проблема ложного success-toast при I/O-ошибке: здесь файл может быть полностью доступен для записи, но исчезает право самого хоста сохранить принадлежащее ему состояние из-за потери сетевой роли.

**Точное основание в зависимости:** в `Worst Hotel Ever/Library/PackageCache/com.unity.netcode.gameobjects@bd2a018756ed/Runtime/Connection/NetworkConnectionManager.cs:656–673` callback предшествует автоматическому shutdown; в `Runtime/Core/NetworkManager.cs:1646–1669` вызываются уведомления остановки и затем заменяется LocalClient, сбрасывая IsHost. В самой сессии нет обработчика, который сохраняет состояние в этом переходе. `HotelSteamTransport.Fail` действительно поднимает такой event; в него приводят ошибки reliable-отправки и ошибки callback/приёма Steam.

**Воспроизведение для проверки интегратором:**

1. Запустить host, получить сохранение A и внести заметное изменение B после последнего autosave.
2. До следующего autosave вызвать отказ транспорта, например диагностическим вызовом `HotelSteamTransport.Fail` в тестовом сценарии. Дождаться завершения NGO shutdown. Для воспроизводимости диагностический hook предпочтительнее неопределённого отключения физической сети.
3. Проверить, что файл доступен, но `Session.Save()` уже возвращает false из-за `!IsHost`. Закрыть приложение или снова выбрать продолжение.
4. Загрузится A: B потеряно, хотя приложение не аварийно завершалось и актуальный мир оставался в памяти после отказа.

**Минимальное исправление:** хранить владение авторитетным миром отдельно от текущей сетевой роли. При `OnTransportFailure`/`OnPreShutdown` снять сохраняемый snapshot до сброса роли. При неудаче записи удерживать его и разрешать повторное сохранение после остановки транспорта; не уничтожать его следующей командой подключения молча. Не решать это снятием ограничения на запись у обычного клиента.

## F02 — P2: одновременные IP-подключения обходят лимит двух сотрудников

**Основное место:** `Worst Hotel Ever/Assets/Scripts/HotelSession.cs:53–58`.

Связанные места: `HotelSession.cs:178,235`; `HotelSimulation.cs:40–44`; `HotelGame.cs:18–20,64–67`.

Approval считает только уже подключённых клиентов: `ConnectedClientsIds.Count < 2`. NGO сохраняет результаты approval и применяет их позже. Если при одном хосте два запроса подключения обрабатываются в одной входящей очереди, оба callback видят Count=1 и одобряют последнее свободное место. На следующем этапе NGO соединяет обоих. `HotelSimulation.Join` для второго удалённого сотрудника молча отказывает из-за `players.Count >= 2`, но Session не отключает этот NGO-клиент и продолжает отправлять ему весь мир. У него Connected=true, создаётся локальный контроллер, но нет собственного PlayerState и команды отклоняются как от неподключённого игрока.

**Доказательство порядка NGO 2.7.0:** `NetworkConnectionManager.cs:794–807` кладёт ответ в ClientsToApprove и вызывает callback без AddClient; `:813–828` позднее обрабатывает все готовые ответы; `:897` добавляет клиента. В `NetworkManager.cs:343,349` `ProcessPendingApprovals` идёт до `ProcessIncomingMessageQueue`, поэтому два запроса из одной очереди видят прежний connected count. Эти файлы находятся в том же каталоге PackageCache, указанном в F01.

**Воспроизведение для проверки интегратором:** один host, затем два IP-клиента с верными protocol/password; обеспечить поступление обоих ConnectionRequest в один EarlyUpdate хоста, например синхронным стартом и управляемой задержкой обработки в тесте. Сравнить `Manager.ConnectedClientsIds.Count` (3) с `State.players.Count` (2); один клиент получает мир без локального сотрудника. Обычный smoke с единственным удалённым клиентом не покрывает эту гонку. Steam-транспорт отдельно ограничивает `peers.Count`, поэтому находка относится прежде всего к IP-пути.

**Минимальное исправление:** резервировать слот для одобренного client ID непосредственно в approval callback и учитывать эти резервы вместе с подключёнными; освобождать на connected/disconnected/timeout/shutdown. Дополнительно проверять успешное появление PlayerState в OnConnected и отключать лишнего NGO-клиента, если симуляция его не приняла. Это не доказательство обхода host-only команд: для такого обхода оснований не найдено.

## F03 — P2: временная ошибка чтения может откатить здоровый save к старому backup

**Основное место:** `Worst Hotel Ever/Assets/Scripts/HotelSaveStore.cs:209–214`.

Связанные места: `HotelSaveStore.cs:198–203,155–158,179`; `HotelSession.cs:74–84,237`.

`ReadRecoverable` возвращает null при любом IOException, в том числе при временном sharing violation здорового primary. `Load` трактует это как отсутствие/повреждение, выбирает более старый backup и запускает мир из него. После снятия внешней блокировки обычное сохранение принимает старое состояние, поскольку worldId совпадает, и перезаписывает здоровый более свежий primary. Первая такая запись ещё оставляет свежую версию в `.bak`; следующая штатная запись перезаписывает и эту резервную копию уже откатившимся состоянием.

**Воспроизведение для native-проверки интегратором:**

1. В тестовом слоте сохранить мир с cash=400, затем тот же мир с cash=350. Получатся primary=350 и backup=400.
2. Удерживать primary открытым отдельным `FileStream` с `FileShare.None`, не меняя ни одного байта. Вызвать Load: исходная ошибка открытия будет IOException, а возвращённое состояние — cash=400 из backup.
3. Снять блокировку. Дважды вызвать Save с загруженным состоянием, как при стартовой записи и очередном autosave: после первой записи primary=400, backup=350; после второй обе копии содержат 400. Здоровое сохранение 350 было принято за повреждённое и утрачено.

**Минимальное исправление:** разрешать автоматический fallback при доказанной ошибке формата/контрольной суммы/структуры и фактическом отсутствии primary. Sharing violation и другие временные I/O-ошибки возвращать вызывающему коду как ошибку чтения с возможностью повторить попытку; не запускать и не сохранять старый мир автоматически. `FileNotFoundException` при гонке удаления можно обработать отдельно от остальных IOException.

## F04 — P2: после незавершённого Steam-запроса в UI нет доступного пути повторной попытки

**Основные места:** `Worst Hotel Ever/Assets/Scripts/HotelSteam.cs:152,161–166,199–212`; `HotelSession.cs:190,245–251`.

На timeout через 15 секунд или отмену `ChangeLobby` намеренно оставляет pendingRequest до завершения native-запроса. Это правильная защита от перекрывающихся операций: следующие запросы отвергаются с требованием вызвать Shutdown. Однако Session после ошибки/отмены вызывает только LeaveLobby. Повторные HostSteam/JoinSteam вызывают InitializeForTest, который для действующего SDK возвращает success без сброса pendingRequest, и снова получают отказ. В интерфейсе нет сброса Steam: штатные вызовы Shutdown находятся на выходе из приложения или в обработке исключения callback.

**Воспроизведение для проверки интегратором:** оставить native create/join-запрос незавершённым дольше 15 секунд либо отменить его до ответа. Пока request не завершён, повторно нажать создание/вход через Steam: вместо нового запроса получается «Steam ещё обрабатывает запрос лобби. Для сброса вызовите Shutdown». Если исходный запрос так и не завершится, пользователю остаётся перезапуск EXE; IP при этом доступен. Сценарий можно проверять контролируемой незавершающейся Task в адаптере теста; фактический зависший Steam-запрос этим ревью не получался.

Ограничение helper явно описывает `docs/reports/steam.md`: pending блокирует retry до ответа или явного Shutdown. Ошибка здесь в отсутствии требуемого пути восстановления у потребителя, а не в самом запрете параллельных native-запросов.

**Минимальное исправление:** при остановленной сетевой сессии дать явный reset/reinitialize Steam после timeout/отмены либо выполнять его на осознанной повторной попытке. Сохранить защиту от перекрывающихся запросов и version guard для поздних результатов; не обнулять только pendingRequest, оставляя старую операцию активной.

## Доказательства и ограничения

Находки F01–F04 переданы интегратору в задачу `01a0c4ca-8d11-7ea0-8736-62c94735d137` через `send_message_to_thread`. Для F03 отдельно уточнено: первая запись сохраняет свежую версию в backup, потеря обеих активных копий происходит после следующей записи.

Все сценарии — статически выведенные контрпримеры, не протокол собственных игровых или native-прогонов. Не проводились: Steam-online, отключение реальной сети, одновременный старт трёх игровых процессов, fault injection в Unity, изменение сохранений пользователя. Ни один чужой PASS не используется как доказательство исполнения этих новых сценариев. Собственная техническая проверка отчёта: `git diff --cached --check`.

В проверенных командных обработчиках sender берётся из NGO, административные команды дополнительно проверяют id=0 в симуляции, а Steam-сокет проверяет членство в lobby и peer SteamId. Новых доказанных способов получить права хоста не найдено. StartNew архивирует прежние файлы и имеет rollback при обычном исключении; нового доказанного дефекта этого rollback в данном проходе не найдено. Отчёт не заявляет, что весь сетевой слой или все аварийные сценарии проверены.

SHA-256 четырёх основных прочитанных файлов, совпадавших с parent `8586d82` во время ревью:

- `HotelSession.cs`: `2130CC44008CE63B8539CC92E8F42E67BBEFEE7F1135814072F5073D4D9675AC`
- `HotelSaveStore.cs`: `2C06737F00D8ED57FB0514E93CF6F643FACD97EFC1C9686733627B5517801F17`
- `HotelSteam.cs`: `82CCAEC321B4D88E0397006DEABEC7CB09371827DD5D513550187050A966324A`
- `HotelSteamTransport.cs`: `9AE1EF662F340896EC3D8602C7EA966440546806722947BD2239FEE9CF7E7195`

## Повторная проверка исправлений — 22 сентября, около 18:19 MSK

Прочитаны незакоммиченные parent-изменения поверх `8586d82`. Родительские правки не переносились и не изменялись. **F01–F04 закрыты по коду. Новых P0/P1 в проверенных recovery-переходах не найдено; runtime-исправления APPROVE в пределах этой проверки. Найденная P2-регрессия F05 затем исправлена и перепроверена; открыто замечание F06 к фильтру ошибок нового fault-теста ниже.** Первоначальный REQUEST CHANGES выше относится к исторической версии.

- **F01:** `HotelSession.cs:21,203,215–222,279–297` сохраняет обязанность записи через OwnsHotel независимо от NGO-роли; OnPreShutdown пытается сохранить и включает NeedsRecovery. Save после shutdown по-прежнему доступен владельцу. Disconnect при ошибке записи возвращает false до очистки состояния даже с аргументом save=false. `HotelGame.cs:106,224` и `HotelUI.cs:45,326–331` удерживают экран восстановления, запрещают обычное закрытие при отказе записи и дают повторить её. `HotelSession.cs:270` останавливает симуляцию во время восстановления/shutdown. Удалённый клиент не получает право записи, поскольку ownsHotel устанавливается только в успешном пути хозяина.
- **F02:** `HotelSession.cs:57–68` объединяет ConnectedClientsIds и approvedClients и резервирует ID непосредственно в callback. `:190–203` освобождает резервы на connected/disconnected; `:193` дополнительно отключает клиента, которого не приняла симуляция; `:297` очищает резервы при завершении сессии. Два запроса одной очереди больше не видят один и тот же свободный слот.
- **F03:** `HotelSaveStore.cs:209–217` оставляет fallback только для InvalidDataException/FileNotFoundException. Sharing violation теперь выходит наружу; старый backup не запускается вместо временно заблокированного healthy primary.
- **F04:** `HotelUI.cs:323` предоставляет явный сброс; `HotelSession.cs:226–229` разрешает его только без Manager, подключения и принадлежащего сессии мира, затем вызывает HotelSteam.Shutdown. Защита поздних native-ответов сохраняется, но пользователь теперь может выйти из описанного состояния pendingRequest без перезапуска EXE.

### F05 — P2: единый Protocol ломает отдельную offline-компиляцию Steam

**Места:** `Worst Hotel Ever/Assets/Scripts/HotelSteam.cs:21` и `Worst Hotel Ever/Assets/Steam/Validation/Verify-Steam.ps1:10,15,21–24,29–32`.

Теперь константа HotelSteam.Protocol ссылается на `HotelSession.Protocol`. Скрипт Verify-Steam формирует `$sources` только из HotelSteam.cs и Steam/Runtime/*.cs; HotelSession.cs в этом наборе нет, а ссылок на Assembly-CSharp также нет. Компилятор не сможет разрешить HotelSession уже при первой сборке; это относится и к NO_STEAM_PLATFORM, потому что константа находится вне платформенного #if. Полная Unity-сборка содержит HotelSession и от этого изменения не ломается.

**Воспроизведение для интегратора:** запустить штатный `Assets/Steam/Validation/Verify-Steam.ps1` с установленными Unity/NGO references. Ожидаемый из перечисленных входов компилятора результат — CS0103 для HotelSession, затем `Compilation failed`, до выполнения offline checks. Скрипт в этой QA-задаче не запускался; сообщение ошибки здесь предсказано по отсутствующему символу, а не скопировано из тестового лога.

**Минимальное исправление:** поместить Protocol в небольшой общий файл без зависимостей от сессии, сослаться на него из HotelSteam и HotelSession и включить этот файл в `$sources` validator. Альтернатива — явно обновить весь необходимый набор исходников/assembly references, сохранив самостоятельность offline-проверки. Повторно компилировать все три конфигурации и offline checks должен интегратор. Находка F05 немедленно передана в основную задачу.

Собственный повторный проход ограничен чтением исходников и проверкой report diff; Unity, EXE, fault injection, native tests и Verify-Steam не запускались. Родитель сообщил, что готовит отдельные регрессионные прогоны; их успех этим отчётом не утверждается.

SHA-256 проверенной незакоммиченной версии:

- `HotelSession.cs`: `304E304575F8D709BFEBE642DB2CD131C394D3A686F55CF23CC9DE4C1843593F`
- `HotelSaveStore.cs`: `AA1034C45D0852DE3F0E22444912D190A7A2870592AE331F46C18E666D3F1BBA`
- `HotelSteam.cs`: `04F0F93472F97E781E6D2A385856D711AB83841913A36F39EF68520D87EDD5C8`
- `HotelGame.cs`: `5B5A7ACAFDC46454EA522EC1E913CDFFF112477FB69555DFD65DC4630E222A91`
- `HotelUI.cs`: `A4AC162F7CBBA4861421103F48CB859989A5C91C2CA2302EC5ADCFB34921F25A`
- `Verify-Steam.ps1`: `DCF20908B930AB4CB28862C3B0A9C8B0A2143CDB7D585BE1BE3765DC672520C3`

### Уточнение: F05 закрыта; проверен новый session fault-тест

Интегратор устранил F05 во время повторного прохода: в повторно прочитанном `HotelSteam.cs:23` Protocol снова независимая константа `WHE-premvp-2`, а `HotelPresentationTests.cs:13` проверяет равенство с HotelSession.Protocol. Скрытая зависимость standalone Steam validator от игрового assembly устранена по коду; сам validator здесь не запускался.

Дополнительно прочитаны новые `Worst Hotel Ever/Assets/Scripts/HotelSessionSmokeTest.cs` и `Tools/Test-Session.ps1`. Код теста проверяет реальное изменение cash между checkpoint и транспортным отказом, OwnsHotel после NGO shutdown, сохранность состояния при OS write-lock, запрет Disconnect(false)/CanQuit и успешную повторную запись после снятия lock. Deterministic вызовы approval покрывают резервирование до применения ответов; отдельные три процесса и проверка ровно одного принятого клиента дополняют этот сценарий. Скрипт проверяет свежесть отчётов, PASS и exit code. Это проверка структуры сценария, не утверждение о его успешном выполнении.

### F06 — P2: whitelist ожидаемых отказов скрывает посторонние ошибки

**Место:** `Worst Hotel Ever/Assets/Scripts/HotelSessionSmokeTest.cs:24–26`; границы ожидания `:77–79,92–100`.

При expectedIo фильтр принимает любую Error/Exception/**Assert** с подстрокой `IOException` либо `Sharing violation` во всём приложении. Флаг остаётся включённым во время ожидания кадра/секунды и нескольких следующих вызовов. Ошибка другого файла или другой подсистемы в этот интервал добавляет EXPECTED_SAVE_IO_FAILURE вместо записи в errors и может пройти вместе с PASS. Аналогично expectedFailure допускает любое число сообщений о transport shutdown за целую секунду, хотя ожидаемый NGO error синхронен вызову InjectTransportFailure.

**Контрпример для проверки интегратором:** в окне `expectedIo=true` добавить независимый диагностический `Debug.LogError("IOException: unrelated file operation failed")` либо Assert с этим текстом. При остальных исправных assertions такой посторонний отказ будет принят как ожидаемый и не сорвёт PASS. Инъекция этого контрпримера здесь не выполнялась.

**Минимальное исправление:** никогда не разрешать Assert в whitelist; сужать ожидаемый I/O к конкретному исключению записи целевого save-файла и его save-origin/операции, проверять число ожидаемых отказов. Session.Save сейчас вызывает `Debug.LogError(e)`, поэтому нужно учитывать нормальный LogType.Error, а не требовать только LogType.Exception. Для transport error ограничить флаг try/finally вокруг синхронного InjectTransportFailure и требовать один ожидаемый event на инъекцию. В ветке locked primary по текущему сценарию ожидаются три ошибки Save: OnPreShutdown, Disconnect(false), CanQuit; симуляция/autosave в NeedsRecovery остановлены. Ошибки вне этих операций должны оставаться в errors.

F06 передана интегратору. Проверка ResetSteam в этом сценарии подтверждает guard и допустимость сброса остановленной сессии; она не создаёт настоящий зависший native Steam-запрос, поэтому не должна выдаваться за Steam-online recovery-прогон. Новых runtime P0/P1 при чтении теста и его потребителей не выявлено.

Уточняющие SHA-256 на 18:22 MSK: исправленный `HotelSteam.cs` — `388A1ABA872BAA948D2FCC88AFB923A90390903114E31EFCA2C4CBB3966764C2`; проверенный `HotelSessionSmokeTest.cs` — `91D0EA6250FBD9BAB48878346351E1335ADD7B1F4AEB8435DBC390B2DD5FBC52`; `Tools/Test-Session.ps1` — `2829C7FA1A24759B1D087D49E95F0C5E2D698BD0184984B75E60AA058ACB18B6`.
